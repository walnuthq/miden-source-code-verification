// `react-router build` emits the server build as plain JS with no type
// declarations; `createRequestHandler` is what checks its shape.
declare module "miden-source-code-verification-web-viewer/server";
