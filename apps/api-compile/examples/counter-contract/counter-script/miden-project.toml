[package]
name = "counter-script"
version = "0.1.0"

[lib]
kind = "tx-script"
namespace = "miden:base/counter-script@1.0.0"

[dependencies]
miden-core = "*"
miden-protocol = "*"
counter-contract = { path = "../counter-contract" }

[package.metadata.miden.dependencies]
counter-contract = { wit = "../counter-contract/target/generated-wit/" }
